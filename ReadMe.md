API REST em Flask para cotação de fretes por transportadora.

## Rodando local
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
FLASK_APP=app flask run --port 8000